{
  "name": "genixo-restoration-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Capacitor wrapper for the Genixo Restoration Rails/Inertia app",
  "scripts": {
    "assets:brand": "bash ./scripts/generate_brand_assets.sh",
    "cap:doctor": "npx cap doctor",
    "cap:sync": "npx cap sync",
    "cap:add:ios": "npx cap add ios",
    "cap:add:android": "npx cap add android",
    "ios:open": "npx cap open ios",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
