<!DOCTYPE html>
<html>
  <head>
    <% page_title = content_for(:title) || "Genixo Restoration" %>
    <% meta_description = content_for(:meta_description) || "Property restoration operations platform for incidents, daily logs, messaging, photos, and documents." %>
    <% absolute_og_image = "#{request.base_url}/og-genixo-restoration.png" %>
    <title><%= page_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="description" content="<%= meta_description %>">
    <meta name="theme-color" content="#14363e">
    <meta name="apple-mobile-web-app-title" content="Genixo Restoration">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <%= csp_meta_tag %>

    <meta property="og:site_name" content="Genixo Restoration">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= page_title %>">
    <meta property="og:description" content="<%= meta_description %>">
    <meta property="og:image" content="<%= absolute_og_image %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= page_title %>">
    <meta property="twitter:description" content="<%= meta_description %>">
    <meta property="twitter:image" content="<%= absolute_og_image %>">

    <link rel="icon" href="/favicon-32x32.png" sizes="32x32" type="image/png">
    <link rel="icon" href="/favicon-192x192.png" sizes="192x192" type="image/png">
    <link rel="icon" href="/icon.png" sizes="512x512" type="image/png">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag "entrypoints/application.css" %>
    <%= vite_javascript_tag "entrypoints/inertia.tsx" %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
