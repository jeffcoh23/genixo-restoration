<% if @guest %>
You've been invited to view incident details on Genixo Restoration.

Create your account to access the incidents shared with you:
<% else %>
You've been invited to join <%= @invitation.organization.name %> as a <%= @invitation.user_type.titleize %>.
<% end %>

Accept your invitation: <%= @accept_url %>

This invitation expires on <%= @invitation.expires_at.strftime("%B %d, %Y") %>.

--------------------------------------------------

Get the mobile app — take site photos and pull reports from the field.

iPhone or iPad: <%= @ios_app_store_url %>
<% if @android_group_url.present? %>
Android (beta — two steps, in order):
  1. Join the tester group: <%= @android_group_url %>
  2. Then install the app:  <%= @android_opt_in_url %>

After joining the group it can take a few minutes before the install link works.
<% end %>
